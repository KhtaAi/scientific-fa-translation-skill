# بستهٔ آفلاین poppler — نصب‌کننده در صورت نبود، خودش دانلود می‌کند
# (نگه داشتن آن مخزن را ۴۲ مگابایت می‌کند و کلون را کند)
payload/poppler/*.zip

# ساخته‌شده به‌صورت محلی
*.bak
verify-*.png
doc.pdf
*-out.txt
__pycache__/
*.pyc
